using UnityEngine;

namespace Doulingo.Gameplay
{
    /// <summary>
    /// Simple component for beat line visual representation.
    /// Static line that gets instantiated once and never changes.
    /// </summary>
    public class BeatLine : MonoBehaviour
    {
        // No serialized fields needed - everything is set once during creation
        // No methods needed - just a simple component for identification and pooling
    }
}
